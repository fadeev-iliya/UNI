/*
  ServoControl — сервопривод: углы, подключение и отключение.

  servo(port, angle)  — установить угол 0..180. При первом вызове
                        порт подключается автоматически.
  servoDetach(port)   — отключить: импульсы прекращаются, привод
                        расслабляется и не держит нагрузку. Так серво
                        не ломается, если механизм упирается или на
                        нем висит груз, а поднятое - мягко опускается.
  servoAttach(port)   — (опционально) подключить заранее, без угла.

  Подключение: сервопривод на порту P3.
*/

#include <UNI.h>

UniBase robot;
UniDev module;

void setup() {
  robot.begin("Servo");
}

void loop() {
  // Рабочий цикл: серво включено и держит углы
  robot.displayPrint("Servo", "ON");
  module.servo(P3, 0);
  delay(1000);
  module.servo(P3, 90);
  delay(1000);
  module.servo(P3, 180);
  delay(1000);

  // Отключаем: привод обмякает, механизм мягко опускается.
  // Попробуйте повернуть качалку рукой - сопротивления нет
  robot.displayPrint("Servo", "OFF");
  module.servoDetach(P3);
  delay(3000);

  // Следующий servo() в начале цикла снова включит привод
}
