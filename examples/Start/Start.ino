/*
  Start — минимальная программа для платформы UNI.

  Создает робота и инициализирует периферию: моторы, энкодеры,
  экран, светодиод и батарею. На экране появится имя робота,
  одометрия и заряд.

  begin() можно не вызывать: любая первая команда (движение,
  экран, датчики) выполнит инициализацию автоматически.
  Имя робота задается в конструкторе, в begin("Имя") или
  не задается вовсе - тогда останется стандартное.

  Пишите свой код в setup() и loop().
*/

#include <UNI.h>

UniBase robot;
UniDev module;

void setup() {
  robot.begin("UNI");

  // Ваш код, выполняется один раз. Например:
  // robot.moveDist(50, 300);
  // robot.rotate(50, 90);
}

void loop() {
  // Ваш код, выполняется по кругу
}
